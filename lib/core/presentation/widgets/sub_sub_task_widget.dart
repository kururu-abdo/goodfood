
import 'package:flutter/material.dart';

class SubSubTaskWidget extends StatelessWidget {
  const SubSubTaskWidget({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card(
      margin: EdgeInsets.only(
        bottom: 6 
      ),

      
    );
  }
}